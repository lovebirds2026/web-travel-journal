name: Deploy Web Travel Journal [DEV]

# Trigger
on:
  push:
    branches: [ dev-deni ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # Won't work on the cloud without additional setup, skip
    # - name: Checkout code
    #   uses: actions/checkout@v3

    # - name: Set up Python
    #   uses: actions/setup-python@v4
    #   with:
    #     python-version: '3.13'

    # - name: Install dependencies
    #   run: |
    #     python -m pip install --upgrade pip
    #     pip install -e .[test]

    # - name: Run tests
    #   run: |
    #     python -m pytest || echo "No tests found"

    - name: Deploy to server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSHKEY }}
        port: ${{ secrets.PORT }}
        script: |
          cd "/home/aimebgl3/python/travel-dev"
          ./.scripts/deploy.sh
